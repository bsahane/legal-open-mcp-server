apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: legal-mcp-server
  labels:
    app: legal-mcp-server
    component: mcp-server
spec:
  to:
    kind: Service
    name: legal-mcp-server
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
