apiVersion: v1
kind: Service
metadata:
  name: legal-mcp-server
  labels:
    app: legal-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: legal-mcp-server
    component: mcp-server
