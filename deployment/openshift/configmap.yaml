apiVersion: v1
kind: ConfigMap
metadata:
  name: legal-mcp-server-config
  labels:
    app: legal-mcp-server
    component: mcp-server
data:
  MCP_PORT: "5001"
  PYTHON_LOG_LEVEL: "INFO"
  MCP_TRANSPORT_PROTOCOL: "http"
  CORS_ENABLED: "true"
  ENVIRONMENT: "production"
  ENABLE_AUTH: "false"
  SSO_CALLBACK_URL: ""
  SSO_AUTHORIZATION_URL: ""
  SSO_TOKEN_URL: ""
  SSO_INTROSPECTION_URL: ""
  MCP_HOST_ENDPOINT: ""

  # --- Legal MCP Server ---
  DEFAULT_JURISDICTION: "IN"
  DEFAULT_STATE: "Maharashtra"
  DEFAULT_HIGH_COURT: "Bombay"
  LEGAL_DATA_PATH: "/app/data"
  DOCUMENT_STORAGE_PATH: "/app/data/documents"
  ENABLE_CITATION_VERIFICATION: "true"
  # open_data (free, no key) | indian_kanoon (paid, opt-in) | disabled.
  CASE_LAW_SOURCE: "open_data"
  # Synced judgment metadata and cached PDFs. Mount a volume here in production
  # so the corpus survives pod restarts.
  CASE_LAW_DATA_PATH: "/app/data/case_law"
  OPEN_DATA_SC_BUCKET: "indian-supreme-court-judgments"
  OPEN_DATA_HC_BUCKET: "indian-high-court-judgments"
  OPEN_DATA_REGION: "ap-south-1"
  # Applies only when CASE_LAW_SOURCE=indian_kanoon.
  INDIAN_KANOON_DAILY_BUDGET_INR: "100"
  # manual | api | disabled. 'manual' returns portal instructions for a human;
  # the official eCourts portal is CAPTCHA-gated and is never automated.
  ECOURTS_ADAPTER: "manual"
  # voyage | local | disabled. 'disabled' falls back to full-text search only.
  EMBEDDING_PROVIDER: "disabled"
  EMBEDDING_MODEL: "voyage-law-2"
  EMBEDDING_DIMENSIONS: "1024"
